# Refuse to run on a Node version this package does not support.
#
# Without it npm proceeds and the scripts fail with whatever the old Node makes
# of a flag it does not know — "node: bad option: --test" rather than a word
# about versions.
engine-strict=true
