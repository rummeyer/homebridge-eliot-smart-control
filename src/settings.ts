/** Must match the `platform` name used in Homebridge's config.json. */
export const PLATFORM_NAME = 'EliotSmartControl';

/** Must match the `name` field in package.json. */
export const PLUGIN_NAME = 'homebridge-eliot-smart-control';
